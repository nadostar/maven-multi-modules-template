# Maven multiple modules template for spring-boot 2.x

## Install
```
git clone https://github.com/nadostar/maven-multi-modules-template.git project
```

## Build
```
mvn clean package -DskipTests=true
# OR
./mvnc clean package -DskipTests=true
```

## Run the application
```
java -jar xx.jar
```

